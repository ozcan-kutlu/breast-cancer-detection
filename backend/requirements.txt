# --- Python (pip) — backend API ---
# Arayüz React + Next.js ile kurulur; paketler requirements.txt içinde değildir.
# Kurulum: cd frontend && npm install   (sürümler: frontend/package.json)
#
fastapi==0.115.6
uvicorn[standard]==0.32.1
scikit-learn==1.5.2
numpy==2.1.3
pydantic==2.10.3
